#include"misc.h"
#include"parser.h"

const char* path = "code.asm";

int main(){
    size_t *size;
    Parser parse("code.asm");
    // if(parse.hasMoreCommands()){
    //     std::cout << "commands" <<std::endl;
    // }
    // else{
    //     std::cout << "no cmnds" <<std::endl;
    // }
    
    return 0;
}